version: 1
backend:
  phases:
    build:
      commands:
        - npm install -g aws-cdk@latest  # Ensure the latest CDK CLI is installed
        - cdk --version                 # Log the version for debugging
        - amplify pull --env staging --yes  # Replace 'staging' with your environment name
        - npm ci
        - npm run build
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
